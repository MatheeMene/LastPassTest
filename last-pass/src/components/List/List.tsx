import React from 'react';

import Card from '../shared/Card/Card';

const List: React.FC = () => {

    return (
        <section>
            <Card />
        </section>
    );
};

export default List;